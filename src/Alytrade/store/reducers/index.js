const ACTIONS = {

}

const initialState = {
    
}

const MainReducer = (state = initialState, action) => {
    const { type, payload } = action
    switch (type) {
        default:
            return state
    }
}

export default MainReducer